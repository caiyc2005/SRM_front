# 用户信息管理模块 — 变更说明

## 变更日期
2026-06-19

## 修改的文件

### 1. `src/components/AppSidebar.vue` — 导航菜单
- 在菜单列表末尾新增 **👥 用户信息管理** 条目
- 点击跳转到 `/user` 路由

### 2. `src/router/index.js` — 路由配置
- 导入 `UserPage.vue` 组件
- 新增路由 `{ path: '/user', name: 'User', component: UserPage }`

### 3. `src/views/user/UserPage.vue` — **新文件**（核心页面）

---

## UserPage.vue 功能详情

### 通用 API 封装（`src/views/user/UserPage.vue:11-42`）
| 函数 | 方法 | 说明 |
|------|------|------|
| `apiGet(action)` | GET | 查询列表数据 |
| `apiPost(action, body)` | POST | 添加数据 |
| `apiPut(action, body)` | PUT | 修改数据 |
| `apiDelete(action, body?)` | DELETE | 删除数据 |

### 标签页1：👤 用户管理

| 功能 | 前端操作 | 后端 API | 说明 |
|------|---------|----------|------|
| 添加用户 | 点击「添加用户」→ 填写表单 → 确定 | `POST AddUser` | 校验账号/用户名/密码不为空 |
| 修改用户 | 点击「修改」→ 编辑表单 → 确定 | `PUT UpdateUser` | 密码留空则不修改 |
| 删除用户 | 点击「删除」→ 确认弹窗 | `DELETE DeleteUser/{id}` | 软删除（设置 IsDel = true） |
| 搜索 | 输入搜索关键字 | 前端过滤 | 按账号/用户名模糊搜索 |

**表单字段：** 账号、用户名、密码、备注

**表格列：** 账号、用户名、备注、状态（正常/停用）、操作（修改/删除）

### 标签页2：🔐 角色管理

| 功能 | 前端操作 | 后端 API | 说明 |
|------|---------|----------|------|
| 添加角色 | 点击「添加角色」→ 填写表单 → 确定 | `POST AddRole` | 校验角色名称不为空 |
| 删除角色 | 点击「删除」→ 确认弹窗 | `DELETE DeleteRole/{id}` | 有关联用户时阻止删除 |

**表单字段：** 角色名称、备注

**表格列：** 角色名称、备注、操作（删除）

### 标签页3：🔗 用户角色关联

| 功能 | 前端操作 | 后端 API | 说明 |
|------|---------|----------|------|
| 添加用户到角色 | 点击「添加用户到角色」→ 选择用户/角色 → 确定 | `POST AddUserToRole` | 下拉选择用户和角色 |
| 移除用户 | 点击「移除」→ 确认弹窗 | `DELETE RemoveUserFromRole` | 传入 userID + roleID |

**表格列：** 用户、角色、操作（移除）

---

## 涉及的后端 API 清单

| 方法 | 路径 | 请求体 | 说明 | 状态 |
|------|------|--------|------|------|
| POST | `/api/User/AddUser` | `{ userCode, userName, password, memo }` | 添加用户 | ✅ 已有 |
| PUT | `/api/User/UpdateUser` | `{ id, userCode, userName, password?, memo }` | 修改用户 | ❌ 需要补充 |
| DELETE | `/api/User/DeleteUser/{id}` | — | 删除用户（软删除） | ✅ 已有 |
| GET | `/api/User/GetUsers` | — | 获取用户列表 | ❌ 需要补充 |
| POST | `/api/User/AddRole` | `{ roleName, memo }` | 添加角色 | ✅ 已有 |
| DELETE | `/api/User/DeleteRole/{id}` | — | 删除角色 | ✅ 已有 |
| GET | `/api/User/GetRoles` | — | 获取角色列表 | ❌ 需要补充 |
| POST | `/api/User/AddUserToRole` | `{ userID, roleID }` | 添加用户到角色 | ✅ 已有 |
| DELETE | `/api/User/RemoveUserFromRole` | `{ userID, roleID }` | 从角色移除用户 | ✅ 已有 |
| GET | `/api/User/GetUserRoles` | — | 获取用户角色关联列表 | ❌ 需要补充 |

> **「状态」说明：** ✅ 已有 = 您提供的后端代码中已实现；❌ 需要补充 = 前端已对接但后端待实现

---

## 后端补充代码参考

以下接口需要您在 `UserController` 中补充：

### 1. GetUsers — 获取用户列表

```csharp
[HttpGet]
public async Task<ActionResult<ApiResult>> GetUsers()
{
    var users = await _context.Users
        .Where(u => !u.IsDel)
        .Select(u => new UserResponse
        {
            ID = u.ID,
            UserCode = u.UserCode,
            UserName = u.UserName,
            IsDel = u.IsDel,
            Memo = u.Memo
        })
        .ToListAsync();

    return Ok(ApiResult.Ok("查询成功", users));
}
```

### 2. UpdateUser — 修改用户

```csharp
[HttpPut]
public async Task<ActionResult<ApiResult>> UpdateUser([FromBody] UpdateUserRequest request)
{
    var user = await _context.Users.FindAsync(request.ID);
    if (user == null)
        return NotFound(ApiResult.Fail("用户不存在"));

    user.UserCode = request.UserCode ?? user.UserCode;
    user.UserName = request.UserName ?? user.UserName;
    if (!string.IsNullOrWhiteSpace(request.Password))
        user.Password = request.Password;
    user.Memo = request.Memo ?? user.Memo;

    await _context.SaveChangesAsync();

    return Ok(ApiResult.Ok("用户修改成功", new UserResponse
    {
        ID = user.ID,
        UserCode = user.UserCode,
        UserName = user.UserName,
        IsDel = user.IsDel,
        Memo = user.Memo
    }));
}
```

### 3. GetRoles — 获取角色列表

```csharp
[HttpGet]
public async Task<ActionResult<ApiResult>> GetRoles()
{
    var roles = await _context.Roles
        .Where(r => !r.IsDel)
        .Select(r => new RoleResponse
        {
            RoleID = r.RoleID,
            RoleName = r.RoleName,
            Memo = r.Memo
        })
        .ToListAsync();

    return Ok(ApiResult.Ok("查询成功", roles));
}
```

### 4. GetUserRoles — 获取用户角色关联列表

```csharp
[HttpGet]
public async Task<ActionResult<ApiResult>> GetUserRoles()
{
    var userRoles = await _context.UserRoles
        .Select(ur => new UserRoleResponse
        {
            ID = ur.ID,
            UserID = ur.UserID,
            RoleID = ur.RoleID
        })
        .ToListAsync();

    return Ok(ApiResult.Ok("查询成功", userRoles));
}
```

### 补充 DTO 定义

```csharp
// UpdateUserRequest
public class UpdateUserRequest
{
    public string ID { get; set; }
    public string UserCode { get; set; }
    public string UserName { get; set; }
    public string? Password { get; set; }
    public string? Memo { get; set; }
}

// UserRoleResponse
public class UserRoleResponse
{
    public string ID { get; set; }
    public string UserID { get; set; }
    public string RoleID { get; set; }
}
```

---

## 前端项目约定

- **UI 框架：** Element Plus（已全局注册，含中文语言包）
- **图标库：** `@element-plus/icons-vue`（已全局注册）
- **路由模式：** `createWebHistory()`
- **API 代理：** Vite 配置将 `/api/*` 代理到 `https://localhost:7221`，路径重写去掉 `/api` 前缀
- **布局模式：** 左侧 `AppSidebar` + 右侧 `main`（顶栏 header + 内容区 content）
